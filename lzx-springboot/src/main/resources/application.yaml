spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/linzx_learning?serverTimezone=GMT%2B8&characterEncoding=utf-8&userSSL=false
    username: root
    password: root

mybatis-plus:
  configuration:
    log-impl:
  mapper-locations:
    - classpath:/mybatisplus/mapper/*.xml